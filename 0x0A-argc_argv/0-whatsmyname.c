#include "main.h"

/**
 * main - Entry point prints the function name
 * @argc: number of argumnets
 * @argv: values
 * Return: Always 0 (Sucsees)
 */

void main(int argc, char const *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
